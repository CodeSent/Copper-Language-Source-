#pragma once
#include <string>
using std::string;

struct {
    string IF = "if";
    string ELSE = "else";
    string WHILE = "while";
    string FOR = "for";
    string SET = "set";
} ReservedKeywords;
